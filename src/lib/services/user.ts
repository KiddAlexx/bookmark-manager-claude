// Phase 1 stub — user service is a no-op until Phase 2 (Auth.js + Neon)
// The interface is established here so the rest of the app never imports
// directly from an auth library.

export function getCurrentUserId(): string {
  // Phase 2: return the authenticated user's ID from the session
  return ""
}
