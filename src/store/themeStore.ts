// Theme state is managed by next-themes (SSR flash prevention, system preference,
// localStorage persistence). Import useTheme from here for a consistent module path.
export { useTheme as useThemeStore } from "next-themes"
